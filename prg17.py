# Write a program that creates a list of integers and then sorts the list in descending order. Print the sorted list.
integer_list = [10, 5, 8, 13, 2, 7]
sorted_list = sorted(integer_list, reverse=True)
print(sorted_list)
